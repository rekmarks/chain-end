
const assert = require('assert')

const { getMetadata, sourceFiles } = require('../index.js')

describe('joinSourceFiles', () => {

  it('getMetadata returns results', async () => {

    // TODO

    return true
  })

  it('joinSourceFiles', () => {

    // TODO

    const metadata = require('./metadata/metadata.json')
    const filepaths = require('./metadata/filepaths.json')

    return true
  })
})
